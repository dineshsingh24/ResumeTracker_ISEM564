runtime: python312
entrypoint: uvicorn backend.main:app --host 0.0.0.0 --port $PORT

env_variables:
  # Set real secrets in GCP Console → App Engine → Settings → Environment variables
  APP_JWT_SECRET: change-me-in-production
